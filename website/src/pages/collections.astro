---
import BaseLayout from '../layouts/BaseLayout.astro';
import Modal from '../components/Modal.astro';
---


<BaseLayout title="Coleções" description="Coleções de prompts, instructions e agentes para workflows específicos" activeNav="collections">
  <main id="main-content">
    <div class="page-header">
      <div class="container">
        <h1>📦 Collections</h1>
        <p>Coleções de prompts, instructions e agentes para workflows específicos</p>
      </div>
    </div>

    <div class="page-content">
      <div class="container">
        <div class="search-bar">
          <label for="search-input" class="sr-only">Buscar collections</label>
          <input type="text" id="search-input" placeholder="Buscar collections..." autocomplete="off">
        </div>

        <div class="filters-bar" id="filters-bar">
          <div class="filter-group">
            <label for="filter-tag">Tag:</label>
            <select id="filter-tag" multiple aria-label="Filtrar por tag"></select>
          </div>
          <div class="filter-group">
            <label class="checkbox-label">
              <input type="checkbox" id="filter-featured">
              Apenas Featured
            </label>
          </div>
          <button id="clear-filters" class="btn btn-secondary btn-small">Limpar filtros</button>
        </div>

        <div class="results-count" id="results-count" aria-live="polite"></div>
        <div class="resource-list" id="resource-list" role="list">
          <div class="loading" aria-live="polite">Carregando collections...</div>
        </div>
      </div>
    </div>
  </main>

  <Modal />

  <script>
    import '../scripts/pages/collections';
  </script>
</BaseLayout>
